/** Vault-local MCP registry for Yapi (never reads ~/.config/mcp or host IDE configs). */
export const YAPI_MCP_CONFIG_PATH = '.yapi/mcp.json';

/** Vault-local OAuth tokens and PKCE state (never ~/.pi/agent/mcp-oauth). */
export const YAPI_MCP_OAUTH_DIR = '.yapi/mcp-oauth';
